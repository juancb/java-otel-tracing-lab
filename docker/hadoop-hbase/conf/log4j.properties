# log4j 1.x config for HBase + Hadoop. Console appender at INFO with a
# pattern that includes the MDC keys the OTel Java agent's log4j-mdc
# instrumentation populates inside a span. Field names match logback
# (trace_id, span_id, trace_flags) so the same Loki regex catches both.

log4j.rootLogger=INFO, console

log4j.appender.console=org.apache.log4j.ConsoleAppender
log4j.appender.console.target=System.err
log4j.appender.console.layout=org.apache.log4j.PatternLayout
log4j.appender.console.layout.ConversionPattern=%d{HH:mm:ss} %-5p [%c{1}] traceId=%X{trace_id} spanId=%X{span_id} %m%n

# Quiet down the noisier subsystems.
log4j.logger.org.apache.zookeeper=WARN
log4j.logger.org.apache.hadoop.hbase.zookeeper=WARN
log4j.logger.org.apache.hadoop.metrics2=WARN
log4j.logger.org.apache.hadoop.hdfs=INFO
log4j.logger.org.apache.hadoop.ipc=INFO
log4j.logger.io.opentelemetry=INFO
