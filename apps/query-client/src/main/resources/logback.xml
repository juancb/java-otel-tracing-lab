<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <!-- traceId / spanId are populated by the OTel agent's logback-mdc-1.0
           instrumentation while inside an active span (e.g. during the
           HBase Scan / Get / Increment auto-instrumented span). Outside a
           span they render empty via logback's `:-` default-if-missing. -->
      <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] traceId=%X{trace_id:-} spanId=%X{span_id:-} %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>

  <logger name="org.apache.hadoop" level="WARN"/>
  <logger name="org.apache.zookeeper" level="WARN"/>
  <logger name="org.apache.hbase" level="INFO"/>
  <logger name="io.opentelemetry" level="INFO"/>
</configuration>
