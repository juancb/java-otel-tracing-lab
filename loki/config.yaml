# Loki single-binary configuration for the OTel lab.
# All-in-one mode: ingester + distributor + querier + querier-frontend in
# one process. Local filesystem storage with TSDB indexes. No auth.
#
# Data lives at /loki/ — that path is owned by uid 10001 in the image, so
# the named volume mounted at /loki inherits the right ownership. Mounting
# the volume at /var/loki (which doesn't exist in the image) instead made
# Docker create the path root-owned, and Loki couldn't mkdir its subdirs.

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9095
  log_level: info

common:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory
  replication_factor: 1
  path_prefix: /loki

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /loki/tsdb-active
    cache_location: /loki/tsdb-cache
  filesystem:
    directory: /loki/chunks

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  retention_delete_delay: 2h
  delete_request_store: filesystem

limits_config:
  retention_period: 72h
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  allow_structured_metadata: true

ruler:
  alertmanager_url: ""
